# Application Use Case Catalog
**Doc ID:** ASS-002
**Version:** v1.0
**Statut:** SPECIFICATION CANONIQUE DEFINIE PAR GENESIS
**Date:** 2026-07-25
**Generateur :** service-generator v1.0 (spec-only)
**Source canonique :** ["DOC-012", "DOC-014", "DOC-015", "API-CONTRACT-001", "API-CONTRACT-002", "API-CONTRACT-004", "API-CONTRACT-005"]
**Transformation_rule :** "application-service-specifier v1.0"
**architecture_version :** "v1.0 (DOC-000-DOC-024 + ARA-v1)"
**compliance_status :** "COMPLIANT"

---

## PRINCIPLE

This document catalogs **ALL 83 operations** (57 Commands + 26 Queries) defined in API-CONTRACT-001 as detailed use cases. Each use case traces to its source command/event in DOC-014, its invariant guards from DOC-015, its RBAC roles from API-CONTRACT-004, and its error codes from API-CONTRACT-005.

No use case was invented. If an operation is not listed, it does not exist.

---

## ORGANIZATION AGGREGATE USE CASES

### UC-ORG-01 — CreateOrganization

| Propriete | Valeur |
|-----------|--------|
| Operation ID | UC-ORG-01 |
| Source API Contract | API-CONTRACT-001 §1 |
| Source Command/Event | DOC-014 CreateOrganization / OrganizationCreated |
| Aggregate | OrganizationAggregate |
| Type | COMMAND |
| RBAC Roles | superadmin (organization:create) |

**Objectif**: Create a new top-level organization with auto-generated org_id.

**Preconditions**:
1. User is authenticated as SuperAdmin
2. No org_id needed (auto-generated by system)
3. Name is non-empty string
4. Type is valid enum (church/school/ngo/company/custom)

**Domain Steps**:
1. Invoke CreateOrganization command on OrganizationAggregate
2. Verify INV-004 (multi-tenant isolation via auto-generated org_id)
3. Capture OrganizationCreated event

**Postconditions**:
1. Organization created with auto-generated org_id
2. Default settings applied per template
3. Event published to bus

**Invariant Guards (DOC-015)**:
- INV-004 (org isolation)

**Domain Events Emitted (DOC-014)**:
- OrganizationCreated: {orgId, name, type, settings}

**Responses**:
- Success: void
- Error codes: E-400-001 (name missing), E-400-004 (invalid type), E-401-001 (not authenticated), E-403-001 (insufficient permission)

**Cross-Aggregate Dependencies**: NONE (foundational aggregate)

**Idempotence**: NO — creating an organization with the same name twice should produce two distinct orgs (each with unique auto-generated org_id)

---

### UC-ORG-02 — UpdateOrganizationSettings

| Propriete | Valeur |
|-----------|--------|
| Operation ID | UC-ORG-02 |
| Source API Contract | API-CONTRACT-001 §2 |
| Source Command/Event | DOC-014 UpdateOrganizationSettings / SettingUpdated |
| Aggregate | OrganizationAggregate |
| Type | COMMAND |
| RBAC Roles | admin (config:update:org) |

**Objectif**: Update one or more organization configuration settings.

**Preconditions**:
1. User authenticated; org_id resolved from session
2. User has config:update:org permission
3. Setting key exists in configuration schema

**Domain Steps**:
1. Load ConfigurationAggregate (settings are org-scoped through Organization context)
2. Invoke UpdateOrganizationSettings(key, value)
3. Verify CFG-001, CFG-002, CFG-003, CFG-004

**Postconditions**: Settings updated with validated formats

**Invariant Guards (DOC-015)**:
- CFG-001 (Currency ISO 4217)
- CFG-002 (Timezone IANA)
- CFG-003 (Accent hex + WCAG contrast)
- CFG-004 (default fallback available)

**Domain Events Emitted (DOC-014)**:
- SettingUpdated: {settingKey, oldValue, newValue, updatedAt}

**Responses**:
- Success: void
- Error codes: E-403-001, E-422-001-CFG-001, E-422-001-CFG-002, E-422-001-CFG-003

**Cross-Aggregate Dependencies**: OrganizationAggregate (org_id context)

**Idempotence**: PARTIAL — updating the same key to the same value is idempotent; different values replace previous

---

### UC-ORG-03 — CreateOrgUnit

| Propriete | Valeur |
|-----------|--------|
| Operation ID | UC-ORG-03 |
| Source API Contract | API-CONTRACT-001 §3 |
| Source Command/Event | DOC-014 CreateOrgUnit / OrgUnitCreated |
| Aggregate | OrganizationAggregate |
| Type | COMMAND |
| RBAC Roles | admin (organization_unit:create) |

**Objectif**: Create a new organizational unit within an existing org hierarchy.

**Preconditions**:
1. Org exists
2. Unit name non-empty, unit_type valid enum
3. Parent reference valid if provided
4. Depth will be ≤5 after creation

**Domain Steps**:
1. Load OrganizationAggregate for target org
2. Invoke CreateOrgUnit(name, parent, unitType)
3. Verify REL-001 (no cycles), REL-002 (depth ≤5), BR-ORG-002

**Postconditions**: New OrgUnit at valid depth in hierarchy

**Invariant Guards (DOC-015)**:
- REL-001 (DAG no cycles)
- REL-002 (depth ≤5)
- BR-ORG-002 (max depth 5 levels)

**Domain Events Emitted (DOC-014)**:
- OrgUnitCreated: {orgUnitId, parentId, unitType, depthLevel}

**Responses**:
- Success: void
- Error codes: E-409-003 (invalid transition), E-422-001-REL-001, E-422-001-REL-002

**Cross-Aggregate Dependencies**: RelationshipAggregate (parent link stored here)

**Idempotence**: NO — each create produces a unique org unit

---

### UC-ORG-04 — UpdateOrgUnitParent

| Propriete | Valeur |
|-----------|--------|
| Operation ID | UC-ORG-04 |
| Source API Contract | API-CONTRACT-001 §4 |
| Source Command/Event | DOC-014 UpdateOrgUnitParent / OrgUnitParentChanged |
| Aggregate | OrganizationAggregate |
| Type | COMMAND |
| RBAC Roles | superadmin (organization_unit:reparent) |

**Objectif**: Change the parent of an organizational unit.

**Preconditions**: Unit exists; new parent exists; no cycle created

**Domain Steps**:
1. Load OrganizationAggregate
2. Invoke UpdateOrgUnitParent(unitId, newParentId)
3. Verify REL-001 (Kahn's algo — no cycles), REL-002 (depth ≤5 after change)

**Postconditions**: Parent link changed; depth revalidated

**Invariant Guards (DOC-015)**:
- REL-001 (DAG no cycles)
- REL-002 (depth ≤5)
- BR-ORG-003 (cannot become own parent)

**Domain Events Emitted (DOC-014)**:
- OrgUnitParentChanged: {orgUnitId, oldParent, newParent, depthLevel}

**Responses**:
- Success: void
- Error codes: E-422-001-REL-001, E-422-001-REL-002

**Cross-Aggregate Dependencies**: RelationshipAggregate (parent links managed here)

**Idempotence**: NO

---

### UC-ORG-05 — TransferChildOrg

| Propriete | Valeur |
|-----------|--------|
| Operation ID | UC-ORG-05 |
| Source API Contract | API-CONTRACT-001 §5 |
| Source Command/Event | DOC-014 TransferChildOrg / ChildOrgTransferred |
| Aggregate | OrganizationAggregate |
| Type | COMMAND |
| RBAC Roles | superadmin (organization:transfer) |

**Objectif**: Re-parent a child organization within the DAG structure.

**Preconditions**: Child org exists; new parent exists; no cycle

**Domain Steps**:
1. Load OrganizationAggregate
2. Invoke TransferChildOrg(childId, newParentId)
3. Verify REL-001 (no cycle created)

**Postconditions**: Child org re-parented in DAG; memberships preserved (BR-REL-003)

**Invariant Guards (DOC-015)**:
- REL-001 (DAG no cycles)

**Domain Events Emitted (DOC-014)**:
- ChildOrgTransferred: {childOrgId, newParentId}

**Responses**:
- Success: void
- Error codes: E-422-001-REL-001

**Cross-Aggregate Dependencies**: RelationshipAggregate (memberships transferred via BR-REL-003)

**Idempotence**: NO

---

### UC-ORG-06 — MergeOrganizations

| Propriete | Valeur |
|-----------|--------|
| Operation ID | UC-ORG-06 |
| Source API Contract | API-CONTRACT-001 §6 |
| Source Command/Event | DOC-014 MergeOrganizations / ChildOrgMerged |
| Aggregate | OrganizationAggregate |
| Type | COMMAND |
| RBAC Roles | superadmin (organization:merge) |

**Objectif**: Merge two organizations into one, archiving the source.

**Preconditions**: Two orgs exist; both within same inheritance DAG

**Domain Steps**:
1. Load OrganizationAggregate
2. Invoke MergeOrganizations(sourceId, targetId)
3. Verify INV-004 (isolation preserved post-merge)

**Postconditions**: Source merged into target; source marked archived

**Invariant Guards (DOC-015)**:
- INV-004 (org isolation)

**Domain Events Emitted (DOC-014)**:
- ChildOrgMerged: {sourceOrgId, targetOrgId}

**Responses**:
- Success: void
- Error codes: E-403-001 (permission), E-404-001 (entity not found)

**Cross-Aggregate Dependencies**: None (self-contained merge)

**Idempotence**: NO

---

### UC-ORG-07 — ArchiveOrganization

| Propriete | Valeur |
|-----------|--------|
| Operation ID | UC-ORG-07 |
| Source API Contract | API-CONTRACT-001 §7 |
| Source Command/Event | DOC-014 ArchiveOrganization / OrganizationArchived |
| Aggregate | OrganizationAggregate |
| Type | COMMAND |
| RBAC Roles | superadmin (organization:archive) |

**Objectif**: Transition an organization to archived status (read-only).

**Preconditions**: Org exists; status = active

**Domain Steps**:
1. Load OrganizationAggregate
2. Invoke ArchiveOrganization()
3. No specific invariants checked

**Postconditions**: Status transitions active → archived; read-only mode

**Invariant Guards (DOC-015)**: NONE explicitly

**Domain Events Emitted (DOC-014)**:
- OrganizationArchived: {orgId, archivedAt}

**Responses**:
- Success: void
- Error codes: E-403-001, E-404-001

**Cross-Aggregate Dependencies**: ResourceAggregate (all resources become read-only)

**Idempotence**: YES — archiving an already-archived org is a no-op

---

### UC-ORG-08 — SuspendOrganization

| Propriete | Valeur |
|-----------|--------|
| Operation ID | UC-ORG-08 |
| Source API Contract | API-CONTRACT-001 §8 |
| Source Command/Event | DOC-014 SuspendOrganization / OrganizationSuspended |
| Aggregate | OrganizationAggregate |
| Type | COMMAND |
| RBAC Roles | superadmin (organization:suspend) |

**Objectif**: Suspend an organization, locking all writes while preserving reads.

**Preconditions**: Org exists; status = active

**Domain Steps**:
1. Load OrganizationAggregate
2. Invoke SuspendOrganization()
3. Verify BR-ORG-006 (suspended org → write locked, read only)

**Postconditions**: Status transitions active → suspended; write locked

**Invariant Guards (DOC-015)**:
- BR-ORG-006 (suspended org → write locked)

**Domain Events Emitted (DOC-014)**:
- OrganizationSuspended: {orgId, suspendedAt, reason}

**Responses**:
- Success: void
- Error codes: E-403-001, E-403-004 (suspended org blocks writes)

**Cross-Aggregate Dependencies**: ResourceAggregate (writes blocked via BR-ORG-006 propagation)

**Idempotence**: YES — suspending an already-suspended org is a no-op

---

### UC-ORG-Q01 — GetOrganizationProfile

| Propriete | Valeur |
|-----------|--------|
| Operation ID | UC-ORG-Q01 |
| Source API Contract | API-CONTRACT-001 §9 |
| Source Command/Event | DOC-014 (queried via resource pattern) / GetOrganizationProfile |
| Aggregate | OrganizationAggregate |
| Type | QUERY |
| RBAC Roles | admin, treasurer, pastor, staff (organization:read:org) |

**Objectif**: Retrieve the profile data of an organization.

**Preconditions**: Org exists; user authenticated

**Domain Steps**:
1. Load OrganizationAggregate for target org_id
2. Verify INV-004 (org_id scoped)

**Postconditions**: Profile data returned

**Invariant Guards (DOC-015)**:
- INV-004 (org isolation)

**Domain Events Emitted (DOC-014)**: NONE

**Responses**:
- Success: Organization profile data
- Error codes: E-404-002 (tenant not found)

**Cross-Aggregate Dependencies**: NONE (read-only)

**Idempotence**: YES — queries are always idempotent

---

### UC-ORG-Q02 — GetDescendantUnits

| Propriete | Valeur |
|-----------|--------|
| Operation ID | UC-ORG-Q02 |
| Source API Contract | API-CONTRACT-001 §10 |
| Source Command/Event | DOC-014 (queried via relationship pattern) / GetDescendantUnits |
| Aggregate | OrganizationAggregate |
| Type | QUERY |
| RBAC Roles | admin (organization_unit:read:org) |

**Objectif**: Return the full descendant tree of a given org unit.

**Preconditions**: rootId exists within same org; depth ≤5

**Domain Steps**:
1. Load OrganizationAggregate
2. Invoke GetDescendantUnits(rootId)
3. Verify REL-002 (depth ≤5 enforced in traversal)

**Postconditions**: Full descendant tree returned

**Invariant Guards (DOC-015)**:
- REL-002 (depth ≤5)

**Domain Events Emitted (DOC-014)**: NONE

**Responses**:
- Success: List of descendant OrgUnits
- Error codes: E-404-001 (root unit not found)

**Cross-Aggregate Dependencies**: RelationshipAggregate (hierarchy data flows through)

**Idempotence**: YES

---

## IDENTITY AGGREGATE USE CASES

### UC-ID-01 — CreateUser

| Propriete | Valeur |
|-----------|--------|
| Operation ID | UC-ID-01 |
| Source API Contract | API-CONTRACT-001 §Identity-1 |
| Source Command/Event | DOC-014 CreateUser / UserCreated |
| Aggregate | IdentityAggregate |
| Type | COMMAND |
| RBAC Roles | superadmin (any role) or admin (treasurer/pastor/staff only) |

**Objectif**: Create a new user within an organization.

**Preconditions**:
1. Org exists
2. Email unique within org
3. Password hash strong (complexity regex)
4. Role respects hierarchy (Admin cannot create superadmin/admin)

**Domain Steps**:
1. Invoke CreateUser(email, password_hash, role) on IdentityAggregate
2. Verify EMAIL-001 (unique by org)
3. Capture UserCreated event

**Postconditions**: New User created with hashed password; role assigned per hierarchy rules

**Invariant Guards (DOC-015)**:
- EMAIL-001 (email unique within org)

**Domain Events Emitted (DOC-014)**:
- UserCreated: {userId, email, role, orgId}

**Responses**:
- Success: void
- Error codes: E-400-002 (duplicate email), E-400-003 (invalid email format), E-403-003 (role violation), E-401-001

**Cross-Aggregate Dependencies**: OrganizationAggregate (org_id context)

**Idempotence**: NO — duplicate email within org rejected by EMAIL-001

---

### UC-ID-02 — UpdateUserProfile

| Propriete | Valeur |
|-----------|--------|
| Operation ID | UC-ID-02 |
| Source API Contract | API-CONTRACT-001 §Identity-2 |
| Source Command/Event | DOC-014 UpdateUserProfile / UserUpdated |
| Aggregate | IdentityAggregate |
| Type | COMMAND |
| RBAC Roles | self (user:update:self) or admin/superadmin (user:update:any) |

**Objectif**: Update selected fields of a user profile.

**Preconditions**: User exists; updates do not violate unique constraints

**Domain Steps**:
1. Load IdentityAggregate
2. Invoke UpdateUserProfile(updates)
3. Verify EMAIL-001 (if email changed — must be unique within org)

**Postconditions**: Selected fields updated

**Invariant Guards (DOC-015)**:
- EMAIL-001 (if email changed)

**Domain Events Emitted (DOC-014)**:
- UserUpdated: {userId, changedFields}

**Responses**:
- Success: void
- Error codes: E-400-002, E-403-001

**Cross-Aggregate Dependencies**: NONE

**Idempotence**: YES — updating same fields to same values is a no-op

---

### UC-ID-03 — ChangeUserRole

| Propriete | Valeur |
|-----------|--------|
| Operation ID | UC-ID-03 |
| Source API Contract | API-CONTRACT-001 §Identity-3 |
| Source Command/Event | DOC-014 ChangeUserRole / UserRoleChanged |
| Aggregate | IdentityAggregate |
| Type | COMMAND |
| RBAC Roles | superadmin only |

**Objectif**: Change a user's role within the RBAC hierarchy.

**Preconditions**: User exists; new role is valid enum

**Domain Steps**:
1. Load IdentityAggregate
2. Invoke ChangeUserRole(newRole)
3. Verify role hierarchy enforced (SuperAdmin only)

**Postconditions**: Role updated per RBAC hierarchy

**Invariant Guards (DOC-015)**: Role hierarchy enforced (SUPER)

**Domain Events Emitted (DOC-014)**:
- UserRoleChanged: {userId, oldRole, newRole, effectiveDate}

**Responses**:
- Success: void
- Error codes: E-403-001, E-404-001

**Cross-Aggregate Dependencies**: NONE

**Idempotence**: YES — setting the same role to itself is a no-op

---

### UC-ID-04 — ResetPassword

| Propriete | Valeur |
|-----------|--------|
| Operation ID | UC-ID-04 |
| Source API Contract | API-CONTRACT-001 §Identity-4 |
| Source Command/Event | DOC-014 ResetPassword / PasswordResetRequested |
| Aggregate | IdentityAggregate |
| Type | COMMAND |
| RBAC Roles | self (password:reset:self) or admin/superadmin (password:reset:any) |

**Objectif**: Reset a user's password to a new strong hash.

**Preconditions**: User exists; new password meets complexity requirements

**Domain Steps**:
1. Load IdentityAggregate
2. Invoke ResetPassword(newPassword_hash)
3. Verify BR-ID-001 (password hash complexity regex)

**Postconditions**: Password hash replaced with new strong hash

**Invariant Guards (DOC-015)**:
- BR-ID-001 (password never plain, hash complexity)

**Domain Events Emitted (DOC-014)**:
- PasswordResetRequested: {userId, resetTimestamp}

**Responses**:
- Success: void
- Error codes: E-400-002 (weak password), E-404-001

**Cross-Aggregate Dependencies**: NONE

**Idempotence**: NO — each reset invalidates prior sessions

---

### UC-ID-05 — LoginUser

| Propriete | Valeur |
|-----------|--------|
| Operation ID | UC-ID-05 |
| Source API Contract | API-CONTRACT-001 §Identity-5 |
| Source Command/Event | DOC-014 LoginUser / UserLoggedIn + SessionCreated |
| Aggregate | IdentityAggregate |
| Type | COMMAND |
| RBAC Roles | N/A (system action — auth:login) |

**Objectif**: Authenticate user credentials and create a session.

**Preconditions**: Valid email/password credentials; correct org_id

**Domain Steps**:
1. Invoke LoginUser(email, password) on IdentityAggregate
2. Verify INV-004 (org_id match), INV-008 (double validation of credentials)
3. Capture UserLoggedIn + SessionCreated events

**Postconditions**: New session created; ephemeral JWT issued

**Invariant Guards (DOC-015)**:
- INV-004 (org_id gate)
- INV-008 (double validation of credentials)

**Domain Events Emitted (DOC-014)**:
- UserLoggedIn: {userId, orgId, deviceInfo, loginTimestamp}
- SessionCreated: {sessionId, refreshTokenHash, expiresAt, userId}

**Responses**:
- Success: Authentication tokens (ephemeral — not stored)
- Error codes: E-401-003 (invalid credentials), E-404-002 (tenant not found)

**Cross-Aggregate Dependencies**: NONE

**Idempotence**: NO — login always creates a new session

---

### UC-ID-06 — LogoutUser

| Propriete | Valeur |
|-----------|--------|
| Operation ID | UC-ID-06 |
| Source API Contract | API-CONTRACT-001 §Identity-6 |
| Source Command/Event | DOC-014 LogoutUser / UserLoggedOut |
| Aggregate | IdentityAggregate |
| Type | COMMAND |
| RBAC Roles | self (auth:logout:self) or superadmin (auth:logout:any) |

**Objectif**: End a user session.

**Preconditions**: Active session exists

**Domain Steps**:
1. Load IdentityAggregate
2. Invoke LogoutUser()
3. No specific invariants checked

**Postconditions**: Session revoked; refresh token hash removed from store

**Invariant Guards (DOC-015)**: NONE

**Domain Events Emitted (DOC-014)**:
- UserLoggedOut: {userId, sessionEndedAt}

**Responses**:
- Success: void
- Error codes: E-404-003 (session not found)

**Cross-Aggregate Dependencies**: NONE

**Idempotence**: YES — logging out an already-logged-out user is a no-op

---

### UC-ID-07 — RefreshAccessToken

| Propriete | Valeur |
|-----------|--------|
| Operation ID | UC-ID-07 |
| Source API Contract | API-CONTRACT-001 §Identity-7 |
| Source Command/Event | DOC-014 RefreshAccessToken / SessionCreated (new refresh) |
| Aggregate | IdentityAggregate |
| Type | COMMAND |
| RBAC Roles | self (auth:refresh) |

**Objectif**: Issue a fresh access token using a valid refresh token.

**Preconditions**: Refresh token valid and not expired

**Domain Steps**:
1. Load IdentityAggregate
2. Invoke RefreshAccessToken()
3. Verify token found and not expired

**Postconditions**: New session created with fresh refresh token hash

**Invariant Guards (DOC-015)**: NONE explicitly

**Domain Events Emitted (DOC-014)**:
- SessionCreated: {sessionId, refreshTokenHash, expiresAt, userId}

**Responses**:
- Success: Authentication tokens
- Error codes: E-401-002 (session expired), E-404-003

**Cross-Aggregate Dependencies**: NONE

**Idempotence**: NO — each refresh generates a new token

---

### UC-ID-08 — RevokeSession

| Propriete | Valeur |
|-----------|--------|
| Operation ID | UC-ID-08 |
| Source API Contract | API-CONTRACT-001 §Identity-8 |
| Source Command/Event | DOC-014 RevokeSession / SessionRevoked |
| Aggregate | IdentityAggregate |
| Type | COMMAND |
| RBAC Roles | self (session:revoke:self) or superadmin (session:revoke:any) |

**Objectif**: Revoke a specific session.

**Preconditions**: Session exists

**Domain Steps**:
1. Load IdentityAggregate
2. Invoke RevokeSession(sessionId)
3. No specific invariants checked

**Postconditions**: Session marked revoked; refresh token deleted

**Invariant Guards (DOC-015)**: NONE

**Domain Events Emitted (DOC-014)**:
- SessionRevoked: {sessionId, revokedBy, revokedAt}

**Responses**:
- Success: void
- Error codes: E-404-003

**Cross-Aggregate Dependencies**: NONE

**Idempotence**: YES — revoking an already-revoked session is a no-op

---

### UC-ID-09 — AssignPermissionGrant

| Propriete | Valeur |
|-----------|--------|
| Operation ID | UC-ID-09 |
| Source API Contract | API-CONTRACT-001 §Identity-9 |
| Source Command/Event | DOC-014 AssignPermissionGrant / (implicit permission update) |
| Aggregate | IdentityAggregate |
| Type | COMMAND |
| RBAC Roles | superadmin only (permission:assign) |

**Objectif**: Assign a permission grant to a role.

**Preconditions**: Role exists; permission string in valid format (resource:action:level)

**Domain Steps**:
1. Load IdentityAggregate
2. Invoke AssignPermissionGrant(permissionString)
3. Wildcard permissions ["*"] audited but authorized

**Postconditions**: Permission grant updated

**Invariant Guards (DOC-015)**: Wildcard permissions audited but authorized

**Domain Events Emitted (DOC-014)**: NONE explicitly (implicit permission update)

**Responses**:
- Success: void
- Error codes: E-400-004 (invalid permission format), E-403-001

**Cross-Aggregate Dependencies**: NONE

**Idempotence**: YES — assigning the same permission twice is a no-op

---

## RESOURCE AGGREGATE USE CASES

### UC-RES-01 — CreateTransaction

| Propriete | Valeur |
|-----------|--------|
| Operation ID | UC-RES-01 |
| Source API Contract | API-CONTRACT-001 §Resource-1 |
| Source Command/Event | DOC-014 CreateTransaction / ResourceCreated |
| Aggregate | ResourceAggregate |
| Type | COMMAND |
| RBAC Roles | admin, treasurer (transaction:create) |

**Objectif**: Create a new financial transaction with validated amount, category, and scope.

**Preconditions**:
1. Org exists
2. Category from vocabulary (CAT-001)
3. Amount > 0 BIGINT cents (FIN-002)
4. Date not in future (DATE-001)
5. Scope type defined (SCOPE-001)
6. createdBy injected from auth context (CREATEBY-001)

**Domain Steps**:
1. Invoke CreateTransaction(data) on ResourceAggregate
2. Verify FIN-002, DATE-001, CAT-001, VERSION-001, CREATEBY-001, SCOPE-001
3. Capture ResourceCreated event

**Postconditions**: New transaction created with auto-increment version=1

**Invariant Guards (DOC-015)**:
- FIN-002 (amount always positive BIGINT)
- DATE-001 (date never future)
- CAT-001 (category from vocabulary)
- VERSION-001 (version always incremented)
- CREATEBY-001 (created_by always set)
- SCOPE-001 (scope always defined)

**Domain Events Emitted (DOC-014)**:
- ResourceCreated: {resourceId, resourceType, orgId, createdBy}

**Responses**:
- Success: void
- Error codes: E-422-001-FIN-002, E-422-001-DATE-001, E-422-001-CAT-001, E-422-001-SCOPE-001, E-422-001-CREATEBY-001

**Cross-Aggregate Dependencies**: VocabularyAggregate (category reference), OrganizationAggregate (org_id)

**Idempotence**: NO — UUID-based dedup possible but not guaranteed

---

### UC-RES-02 — UpdateDraftTransaction

| Propriete | Valeur |
|-----------|--------|
| Operation ID | UC-RES-02 |
| Source API Contract | API-CONTRACT-001 §Resource-2 |
| Source Command/Event | DOC-014 UpdateDraftTransaction / ResourceUpdated |
| Aggregate | ResourceAggregate |
| Type | COMMAND |
| RBAC Roles | admin, creator (transaction:update:draft) |

**Objectif**: Update a draft transaction (only draft state allowed; approved immutable).

**Preconditions**: Transaction in draft status only

**Domain Steps**:
1. Load ResourceAggregate
2. Invoke UpdateDraftTransaction(id, updates)
3. Verify FIN-001 (approved transactions IMMUTABLE — guard rejects non-draft), VERSION-001

**Postconditions**: Draft updated; version incremented

**Invariant Guards (DOC-015)**:
- FIN-001 (approved transactions immutable)
- VERSION-001 (version always incremented)

**Domain Events Emitted (DOC-014)**:
- ResourceUpdated: {resourceId, resourceType, changes (diff), version}

**Responses**:
- Success: void
- Error codes: E-422-001-FIN-001, E-422-001-VERSION-001

**Cross-Aggregate Dependencies**: NONE (self-contained update)

**Idempotence**: NO — each update increments version

---

### UC-RES-03 — SubmitForApproval

| Propriete | Valeur |
|-----------|--------|
| Operation ID | UC-RES-03 |
| Source API Contract | API-CONTRACT-001 §Resource-3 |
| Source Command/Event | DOC-014 SubmitForApproval / ApprovalRequested |
| Aggregate | ResourceAggregate |
| Type | COMMAND |
| RBAC Roles | admin, treasurer, pastor (transaction:submit) |

**Objectif**: Transition a draft transaction to pending approval.

**Preconditions**: Transaction in draft status only

**Domain Steps**:
1. Load ResourceAggregate
2. Invoke SubmitForApproval(id)
3. State transition draft → pending enforced

**Postconditions**: Status transitions draft → pending

**Invariant Guards (DOC-015)**: NONE explicitly

**Domain Events Emitted (DOC-014)**:
- ApprovalRequested: {resourceId, requestedBy, thresholdInfo}

**Responses**:
- Success: void
- Error codes: E-409-003 (invalid state transition)

**Cross-Aggregate Dependencies**: WorkflowAggregate (triggers workflow upon ApprovalRequested event)

**Idempotence**: NO — only draft can be submitted

---

### UC-RES-04 — ApproveTransaction

| Propriete | Valeur |
|-----------|--------|
| Operation ID | UC-RES-04 |
| Source API Contract | API-CONTRACT-001 §Resource-4 |
| Source Command/Event | DOC-014 ApproveTransaction / ResourceStateChanged + ApprovalGranted |
| Aggregate | ResourceAggregate |
| Type | COMMAND |
| RBAC Roles | admin, treasurer (transaction:approve) |

**Objectif**: Approve a pending financial transaction.

**Preconditions**: Transaction in pending status; approver has valid permission grant

**Domain Steps**:
1. Load ResourceAggregate
2. Invoke ApproveTransaction(id, approver)
3. Verify BR-RES-001 (approver has correct permission)
4. Capture ResourceStateChanged + ApprovalGranted

**Postconditions**: Status transitions pending → approved

**Invariant Guards (DOC-015)**:
- BR-RES-001 (approver has correct permission)

**Domain Events Emitted (DOC-014)**:
- ResourceStateChanged: {resourceId, resourceType, oldState, newState, transitionedBy}
- ApprovalGranted: {resourceId, approvedBy, approvedAt, comment}

**Responses**:
- Success: void
- Error codes: E-409-003 (invalid transition), E-403-001

**Cross-Aggregate Dependencies**: WorkflowAggregate (if approval triggers workflow step completion)

**Idempotence**: NO — once approved, transaction becomes immutable

---

### UC-RES-05 — RejectTransaction

| Propriete | Valeur |
|-----------|--------|
| Operation ID | UC-RES-05 |
| Source API Contract | API-CONTRACT-001 §Resource-5 |
| Source Command/Event | DOC-014 RejectTransaction / ResourceStateChanged + ApprovalRejected |
| Aggregate | ResourceAggregate |
| Type | COMMAND |
| RBAC Roles | admin (transaction:reject) |

**Objectif**: Reject a pending financial transaction with required reason.

**Preconditions**: Transaction in pending status; reason required

**Domain Steps**:
1. Load ResourceAggregate
2. Invoke RejectTransaction(id, reason)
3. Verify BR-RES-001

**Postconditions**: Status transitions pending → rejected

**Invariant Guards (DOC-015)**:
- BR-RES-001

**Domain Events Emitted (DOC-014)**:
- ResourceStateChanged: {resourceId, resourceType, oldState, newState, transitionedBy}
- ApprovalRejected: {resourceId, rejectedBy, reason, timestamp}

**Responses**:
- Success: void
- Error codes: E-400-001 (reason missing), E-409-003

**Cross-Aggregate Dependencies**: WorkflowAggregate (if rejection triggers workflow resubmit)

**Idempotence**: NO

---

### UC-RES-06 — CompensateTransaction

| Propriete | Valeur |
|-----------|--------|
| Operation ID | UC-RES-06 |
| Source API Contract | API-CONTRACT-001 §Resource-6 |
| Source Command/Event | DOC-014 CompensateTransaction / TransactionCompensated |
| Aggregate | ResourceAggregate |
| Type | COMMAND |
| RBAC Roles | admin, treasurer (transaction:create — same as create) |

**Objectif**: Create a compensating transaction to correct an approved transaction.

**Preconditions**: Approved transaction exists; compensation linked via compensates_for

**Domain Steps**:
1. Load ResourceAggregate
2. Invoke CompensateTransaction(approvedTxId)
3. Verify COMP-001 (link to original required)

**Postconditions**: New compensating transaction created linked to original

**Invariant Guards (DOC-015)**:
- COMP-001 (compensation link to original)

**Domain Events Emitted (DOC-014)**:
- TransactionCompensated: {originalTxId, compensationTxId, reason}

**Responses**:
- Success: void
- Error codes: E-422-001-COMP-001, E-404-001 (original tx not found)

**Cross-Aggregate Dependencies**: ReportingAggregate (balance recalculated upon this event)

**Idempotence**: NO — each compensation creates a new unique transaction

---

### UC-RES-07 — CreateMember

| Propriete | Valeur |
|-----------|--------|
| Operation ID | UC-RES-07 |
| Source API Contract | API-CONTRACT-001 §Resource-7 |
| Source Command/Event | DOC-014 CreateMember / ResourceCreated |
| Aggregate | ResourceAggregate |
| Type | COMMAND |
| RBAC Roles | admin (member:create) |

**Objectif**: Create a new member record within the organization.

**Preconditions**: Org exists; firstName+lastName mandatory; email unique if provided

**Domain Steps**:
1. Load ResourceAggregate
2. Invoke CreateMember(data)
3. Verify MEM-001 (firstName+lastName), EMAIL-001 (unique by org if email), STATUS-010 (valid initial status)

**Postconditions**: Member created

**Invariant Guards (DOC-015)**:
- MEM-001 (firstName+lastName mandatory)
- EMAIL-001 (unique by org if email provided)
- STATUS-010 (valid initial status)

**Domain Events Emitted (DOC-014)**:
- ResourceCreated: {resourceId, resourceType, orgId, createdBy}

**Responses**:
- Success: void
- Error codes: E-422-001-MEM-001, E-409-002 (email duplicate)

**Cross-Aggregate Dependencies**: OrganizationAggregate (org_id)

**Idempotence**: NO

---

### UC-RES-08 — UpdateMember

| Propriete | Valeur |
|-----------|--------|
| Operation ID | UC-RES-08 |
| Source API Contract | API-CONTRACT-001 §Resource-8 |
| Source Command/Event | DOC-014 UpdateMember / ResourceUpdated |
| Aggregate | ResourceAggregate |
| Type | COMMAND |
| RBAC Roles | admin (member:update) |

**Objectif**: Update member fields.

**Preconditions**: Member exists; firstName+lastName always mandatory after update

**Domain Steps**:
1. Load ResourceAggregate
2. Invoke UpdateMember(id, updates)
3. Verify MEM-001 (firstName+lastName still mandatory)

**Postconditions**: Fields updated

**Invariant Guards (DOC-015)**:
- MEM-001 (firstName+lastName mandatory)

**Domain Events Emitted (DOC-014)**:
- ResourceUpdated: {resourceId, resourceType, changes, version}

**Responses**:
- Success: void
- Error codes: E-422-001-MEM-001, E-404-001

**Cross-Aggregate Dependencies**: NONE

**Idempotence**: YES — updating same values is a no-op

---

### UC-RES-09 — TransitionMemberStatus

| Propriete | Valeur |
|-----------|--------|
| Operation ID | UC-RES-09 |
| Source API Contract | API-CONTRACT-001 §Resource-9 |
| Source Command/Event | DOC-014 TransitionMemberStatus / ResourceStateChanged |
| Aggregate | ResourceAggregate |
| Type | COMMAND |
| RBAC Roles | admin (member:status:transition) |

**Objectif**: Change a member's status per state machine rules.

**Preconditions**: Member exists; status transition is valid per state machine

**Domain Steps**:
1. Load ResourceAggregate
2. Invoke TransitionMemberStatus(id, newStatus)
3. Verify STATUS-010 (only valid states: active/inactive/deceased/transferred)

**Postconditions**: Status transitions per state machine

**Invariant Guards (DOC-015)**:
- STATUS-010 (valid states only)

**Domain Events Emitted (DOC-014)**:
- ResourceStateChanged: {resourceId, resourceType, oldState, newState, transitionedBy}

**Responses**:
- Success: void
- Error codes: E-422-001-STATUS-010, E-409-003

**Cross-Aggregate Dependencies**: RelationshipAggregate (if transfer requires certification TRANS-012)

**Idempotence**: YES — transitioning to same status is a no-op

---

### UC-RES-Q01 — SearchResources

| Propriete | Valeur |
|-----------|--------|
| Operation ID | UC-RES-Q01 |
| Source API Contract | API-CONTRACT-001 §Resource-10 |
| Source Command/Event | DOC-014 SearchResources / — |
| Aggregate | ResourceAggregate |
| Type | QUERY |
| RBAC Roles | admin, treasurer, pastor, staff (resource:read) |

**Objectif**: Search for resources (transactions/members/events) with filters.

**Preconditions**: Read permission on resource type; org_id from context

**Domain Steps**:
1. Load ResourceAggregate
2. Invoke SearchResources(filters)
3. Verify INV-004 (org_id isolated)

**Postconditions**: Filtered resource list returned

**Invariant Guards (DOC-015)**:
- INV-004 (org isolation)

**Domain Events Emitted (DOC-014)**: NONE

**Responses**:
- Success: Resource list (read-only)
- Error codes: E-403-001

**Cross-Aggregate Dependencies**: NONE (read-only filter)

**Idempotence**: YES

---

### UC-RES-Q02 — ExportResources

| Propriete | Valeur |
|-----------|--------|
| Operation ID | UC-RES-Q02 |
| Source API Contract | API-CONTRACT-001 §Resource-11 |
| Source Command/Event | DOC-014 ExportResources / — |
| Aggregate | ResourceAggregate |
| Type | QUERY |
| RBAC Roles | admin, treasurer (reporting:read) |

**Objectif**: Export resource data in a specified format.

**Preconditions**: reporting:read permission; org_id from context

**Domain Steps**:
1. Load ResourceAggregate
2. Invoke ExportResources(format, filters)
3. Verify EXPORT-001 (export timestamped)

**Postconditions**: Export generated with timestamp and digital signature

**Invariant Guards (DOC-015)**:
- EXPORT-001 (export timestamped)

**Domain Events Emitted (DOC-014)**: NONE

**Responses**:
- Success: Export data
- Error codes: E-403-001

**Cross-Aggregate Dependencies**: NONE

**Idempotence**: YES — same filters/format produce same export

---

## RELATIONSHIP AGGREGATE USE CASES

### UC-REL-01 — AddMemberToGroup

| Propriete | Valeur |
|-----------|--------|
| Operation ID | UC-REL-01 |
| Source API Contract | API-CONTRACT-001 §Relationship-1 |
| Source Command/Event | DOC-014 AddMemberToGroup / MemberJoinedGroup |
| Aggregate | RelationshipAggregate |
| Type | COMMAND |
| RBAC Roles | admin (membership:manage) |

**Objectif**: Link a member to an organizational unit/group.

**Preconditions**: Member and group exist; no duplicate membership

**Domain Steps**:
1. Load RelationshipAggregate
2. Invoke AddMemberToGroup(memberId, groupId)
3. Verify MULTI-020 (no duplicate PK)

**Postconditions**: Many-to-many membership created

**Invariant Guards (DOC-015)**:
- MULTI-020 (no duplicate membership PK)

**Domain Events Emitted (DOC-014)**:
- MemberJoinedGroup: {memberId, groupId, joinedAt}

**Responses**:
- Success: void
- Error codes: E-409-004 (duplicate membership), E-404-001

**Cross-Aggregate Dependencies**: NONE (pure relationship)

**Idempotence**: NO — duplicate PK constraint prevents double-add

---

### UC-REL-02 — RemoveMemberFromGroup

| Propriete | Valeur |
|-----------|--------|
| Operation ID | UC-REL-02 |
| Source API Contract | API-CONTRACT-001 §Relationship-2 |
| Source Command/Event | DOC-014 RemoveMemberFromGroup / MemberLeftGroup |
| Aggregate | RelationshipAggregate |
| Type | COMMAND |
| RBAC Roles | admin (membership:manage) |

**Objectif**: Remove a member from an organizational unit/group.

**Preconditions**: Membership exists

**Domain Steps**:
1. Load RelationshipAggregate
2. Invoke RemoveMemberFromGroup(memberId, groupId)
3. Verify HISTORY-022 (history preserved in audit trail)

**Postconditions**: Membership removed; history preserved in audit trail

**Invariant Guards (DOC-015)**:
- HISTORY-022 (historique conserve)

**Domain Events Emitted (DOC-014)**:
- MemberLeftGroup: {memberId, groupId, leftAt}

**Responses**:
- Success: void
- Error codes: E-404-001

**Cross-Aggregate Dependencies**: AuditAggregate (audit trail via LogAction)

**Idempotence**: YES — removing a non-membership is a no-op

---

### UC-REL-03 — SetOrgUnitParent

| Propriete | Valeur |
|-----------|--------|
| Operation ID | UC-REL-03 |
| Source API Contract | API-CONTRACT-001 §Relationship-3 |
| Source Command/Event | DOC-014 SetOrgUnitParent / OrgUnitReparented |
| Aggregate | RelationshipAggregate |
| Type | COMMAND |
| RBAC Roles | superadmin (org_unit:reparent) |

**Objectif**: Change the parent of an organizational unit within the DAG.

**Preconditions**: Both units in same org; no cycle created

**Domain Steps**:
1. Load RelationshipAggregate
2. Invoke SetOrgUnitParent(unitId, parentId)
3. Verify REL-001 (Kahn's algo — no cycles), REL-002 (depth ≤5)

**Postconditions**: Parent link updated; depth revalidated

**Invariant Guards (DOC-015)**:
- REL-001 (DAG no cycles)
- REL-002 (depth ≤5)

**Domain Events Emitted (DOC-014)**:
- OrgUnitReparented: {unitId, oldParentId, newParentId}

**Responses**:
- Success: void
- Error codes: E-422-001-REL-001, E-422-001-REL-002

**Cross-Aggregate Dependencies**: NONE (self-contained relationship update)

**Idempotence**: NO

---

### UC-REL-Q01 — GetDescendants

| Propriete | Valeur |
|-----------|--------|
| Operation ID | UC-REL-Q01 |
| Source API Contract | API-CONTRACT-001 §Relationship-4 |
| Source Command/Event | DOC-014 GetDescendants / — |
| Aggregate | RelationshipAggregate |
| Type | QUERY |
| RBAC Roles | admin (organization_unit:read) |

**Objectif**: Return the full descendant tree of a given organizational unit.

**Preconditions**: Unit exists; max depth 5 enforced

**Domain Steps**:
1. Load RelationshipAggregate
2. Invoke GetDescendants(unitId)
3. Verify REL-002 (depth ≤5 enforced in traversal)

**Postconditions**: Full descendant tree returned

**Invariant Guards (DOC-015)**:
- REL-002 (depth ≤5)

**Domain Events Emitted (DOC-014)**:
- DescendantEnumerationRequested: {rootUnitId, depthRequested}

**Responses**:
- Success: List of descendant units
- Error codes: E-404-001

**Cross-Aggregate Dependencies**: NONE (read-only traversal)

**Idempotence**: YES

---

### UC-REL-Q02 — GetAllGroupsForMember

| Propriete | Valeur |
|-----------|--------|
| Operation ID | UC-REL-Q02 |
| Source API Contract | API-CONTRACT-001 §Relationship-5 |
| Source Command/Event | DOC-014 (queried via relationship pattern) / — |
| Aggregate | RelationshipAggregate |
| Type | QUERY |
| RBAC Roles | admin, treasurer, pastor, staff (membership:read) |

**Objectif**: Query all groups a member belongs to.

**Preconditions**: Member exists

**Domain Steps**:
1. Load RelationshipAggregate
2. Invoke GetAllGroupsForMember(memberId)
3. Verify INV-004 (org isolated)

**Postconditions**: All group memberships returned

**Invariant Guards (DOC-015)**:
- INV-004 (org isolation)

**Domain Events Emitted (DOC-014)**: NONE

**Responses**:
- Success: List of GroupMembership records
- Error codes: E-404-001

**Cross-Aggregate Dependencies**: NONE

**Idempotence**: YES

---

### UC-REL-Q03 — GetAllMembersOfGroup

| Propriete | Valeur |
|-----------|--------|
| Operation ID | UC-REL-Q03 |
| Source API Contract | API-CONTRACT-001 §Relationship-6 |
| Source Command/Event | DOC-014 (queried via relationship pattern) / — |
| Aggregate | RelationshipAggregate |
| Type | QUERY |
| RBAC Roles | admin, treasurer, pastor, staff (membership:read) |

**Objectif**: Query all members belonging to a group.

**Preconditions**: Group exists

**Domain Steps**:
1. Load RelationshipAggregate
2. Invoke GetAllMembersOfGroup(groupId)
3. Verify INV-004 (org isolated)

**Postconditions**: All members of the group returned

**Invariant Guards (DOC-015)**:
- INV-004 (org isolation)

**Domain Events Emitted (DOC-014)**: NONE

**Responses**:
- Success: List of member records with their roles
- Error codes: E-404-001

**Cross-Aggregate Dependencies**: NONE

**Idempotence**: YES

---

## WORKFLOW AGGREGATE USE CASES

### UC-WF-01 — TriggerWorkflow

| Propriete | Valeur |
|-----------|--------|
| Operation ID | UC-WF-01 |
| Source API Contract | API-CONTRACT-001 §Workflow-1 |
| Source Command/Event | DOC-014 TriggerWorkflow / WorkflowTriggered |
| Aggregate | WorkflowAggregate |
| Type | COMMAND |
| RBAC Roles | system (auto) or admin (workflow:trigger) |

**Objectif**: Start a new workflow instance based on trigger event.

**Preconditions**: Workflow definition exists; trigger event matched

**Domain Steps**:
1. Load WorkflowAggregate
2. Invoke TriggerWorkflow(definition, resource)
3. Verify LOG-005 (execution states logged immediately), WF-006 (no new capabilities created)

**Postconditions**: New workflow instance created at step 1

**Invariant Guards (DOC-015)**:
- LOG-005 (execution states logged)
- WF-006 (no new capabilities added to manifest)

**Domain Events Emitted (DOC-014)**:
- WorkflowTriggered: {instanceId, triggerEvent, resourceType, resourceId}

**Responses**:
- Success: void
- Error codes: E-404-006 (workflow definition not found)

**Cross-Aggregate Dependencies**: ResourceAggregate (workflow operates ON a resource), NotificationAggregate (alerts assignees)

**Idempotence**: YES — same trigger event on same resource can be re-triggered safely if idempotency key used

---

### UC-WF-02 — ApproveStep

| Propriete | Valeur |
|-----------|--------|
| Operation ID | UC-WF-02 |
| Source API Contract | API-CONTRACT-001 §Workflow-2 |
| Source Command/Event | DOC-014 ApproveStep / StepApproved |
| Aggregate | WorkflowAggregate |
| Type | COMMAND |
| RBAC Roles | assigned approver role (workflow:approve) |

**Objectif**: Approve a workflow step assigned to the current user.

**Preconditions**: Step is approval type; assigned to this user; timeout valid

**Domain Steps**:
1. Load WorkflowAggregate
2. Invoke ApproveStep(instanceId, userId)
3. Verify CHAINS-003 (approval chain ≤5 levels), WF-005 (never modifies approved transactions directly)

**Postconditions**: Step transitions pending → completed; next step activated

**Invariant Guards (DOC-015)**:
- CHAINS-003 (approval chain ≤5 levels)
- WF-005 (financial workflows do NOT modify approved transactions)

**Domain Events Emitted (DOC-014)**:
- StepApproved: {instanceId, stepId, approvedBy, comment}

**Responses**:
- Success: void
- Error codes: E-422-001-WF-005, E-404-001

**Cross-Aggregate Dependencies**: ResourceAggregate (if approval triggers resource state change)

**Idempotence**: NO — each approval is a distinct action

---

### UC-WF-03 — RejectStep

| Propriete | Valeur |
|-----------|--------|
| Operation ID | UC-WF-03 |
| Source API Contract | API-CONTRACT-001 §Workflow-3 |
| Source Command/Event | DOC-014 RejectStep / StepRejected |
| Aggregate | WorkflowAggregate |
| Type | COMMAND |
| RBAC Roles | assigned approver role (workflow:reject) |

**Objectif**: Reject a workflow step with a required reason.

**Preconditions**: Step is approval type; assigned to this user; reason required

**Domain Steps**:
1. Load WorkflowAggregate
2. Invoke RejectStep(instanceId, userId, reason)
3. Verify WF-001 (timeout still valid), LOG-005

**Postconditions**: Step transitions pending → rejected; reason recorded

**Invariant Guards (DOC-015)**:
- WF-001 (timeout max 30 days)
- LOG-005 (execution states logged)

**Domain Events Emitted (DOC-014)**:
- StepRejected: {instanceId, stepId, rejectedBy, reason}

**Responses**:
- Success: void
- Error codes: E-422-001-WF-001, E-400-001 (reason missing)

**Cross-Aggregate Dependencies**: ResourceAggregate (if rejection returns resource to draft)

**Idempotence**: NO

---

### UC-WF-04 — CancelWorkflow

| Propriete | Valeur |
|-----------|--------|
| Operation ID | UC-WF-04 |
| Source API Contract | API-CONTRACT-001 §Workflow-4 |
| Source Command/Event | DOC-014 CancelWorkflow / WorkflowCancelled |
| Aggregate | WorkflowAggregate |
| Type | COMMAND |
| RBAC Roles | admin or assigned approver (workflow:cancel) |

**Objectif**: Cancel a running workflow instance.

**Preconditions**: Workflow in running status

**Domain Steps**:
1. Load WorkflowAggregate
2. Invoke CancelWorkflow(instanceId, reason)
3. Verify LOG-005 (execution state logged)

**Postconditions**: Workflow transitions running → cancelled

**Invariant Guards (DOC-015)**:
- LOG-005 (execution states logged)

**Domain Events Emitted (DOC-014)**:
- WorkflowCancelled: {instanceId, cancelledBy, reason}

**Responses**:
- Success: void
- Error codes: E-409-003 (cannot cancel non-running workflow)

**Cross-Aggregate Dependencies**: NONE

**Idempotence**: YES — cancelling an already-cancelled workflow is a no-op

---

### UC-WF-05 — ResubmitForApproval

| Propriete | Valeur |
|-----------|--------|
| Operation ID | UC-WF-05 |
| Source API Contract | API-CONTRACT-001 §Workflow-5 |
| Source Command/Event | DOC-014 ResubmitForApproval / WorkflowTriggered (re-trigger) |
| Aggregate | WorkflowAggregate |
| Type | COMMAND |
| RBAC Roles | requester/creator (workflow:resubmit) |

**Objectif**: Resubmit a rejected workflow back to draft/pending state.

**Preconditions**: Workflow rejected or in_revision

**Domain Steps**:
1. Load WorkflowAggregate
2. Invoke ResubmitForApproval(instanceId)
3. Verify RETRY-004 (manual only — no auto-retry code path)

**Postconditions**: Workflow returns to draft/pending; re-triggers

**Invariant Guards (DOC-015)**:
- RETRY-004 (manual only)

**Domain Events Emitted (DOC-014)**:
- WorkflowTriggered (re-trigger): {instanceId, triggerEvent, resourceType, resourceId}

**Responses**:
- Success: void
- Error codes: E-422-001-RETRY-004 (auto-retry blocked), E-409-003

**Cross-Aggregate Dependencies**: ResourceAggregate (returns resource to draft/pending)

**Idempotence**: NO

---

### UC-WF-Q01 — GetPendingApprovals

| Propriete | Valeur |
|-----------|--------|
| Operation ID | UC-WF-Q01 |
| Source API Contract | API-CONTRACT-001 §Workflow-query |
| Source Command/Event | DOC-014 (boundary-defined) / — |
| Aggregate | WorkflowAggregate |
| Type | QUERY |
| RBAC Roles | admin, any assigned approver (workflow:read) |

**Objectif**: Return list of workflow steps awaiting this user's approval.

**Preconditions**: User authenticated; org_id from context

**Domain Steps**:
1. Load WorkflowAggregate
2. Invoke GetPendingApprovals(userId)
3. Verify LOG-005 (execution states logged)

**Postconditions**: List of steps awaiting approval returned

**Invariant Guards (DOC-015)**:
- LOG-005 (execution states logged)

**Domain Events Emitted (DOC-014)**: NONE

**Responses**:
- Success: List of pending approval steps
- Error codes: E-403-001

**Cross-Aggregate Dependencies**: NONE (read-only query)

**Idempotence**: YES

---

## FORM AGGREGATE USE CASES

### UC-FRM-01 — ValidateFormData

| Propriete | Valeur |
|-----------|--------|
| Operation ID | UC-FRM-01 |
| Source API Contract | API-CONTRACT-001 §Form-2 |
| Source Command/Event | DOC-014 ValidateFormData / FormValidationFailed (if invalid) |
| Aggregate | FormAggregate |
| Type | COMMAND (validation only, no mutation) |
| RBAC Roles | any authenticated (form:validate) |

**Objectif**: Validate submitted form data against form definition rules.

**Preconditions**: Form loaded; data submitted

**Domain Steps**:
1. Load FormAggregate
2. Invoke ValidateFormData(formDef, data)
3. Verify DUAL-008 (client validation = server validation exactly)

**Postconditions**: ValidationResult returned (valid/invalid + errors)

**Invariant Guards (DOC-015)**:
- DUAL-008 (client/server validation match)

**Domain Events Emitted (DOC-014)**:
- FormValidationFailed (if invalid): {formId, fieldName, errorCode, userMessage}
- None if valid

**Responses**:
- Success: ValidationResult (valid/invalid + list of errors)
- Error codes: E-422-001-DUAL-008

**Cross-Aggregate Dependencies**: NONE (self-contained validation)

**Idempotence**: YES — same formDef + data always produces same result

---

### UC-FRM-Q01 — LoadFormDefinition

| Propriete | Valeur |
|-----------|--------|
| Operation ID | UC-FRM-Q01 |
| Source API Contract | API-CONTRACT-001 §Form-1 |
| Source Command/Event | DOC-014 LoadFormDefinition / — |
| Aggregate | FormAggregate |
| Type | QUERY |
| RBAC Roles | any authenticated (form:read) |

**Objectif**: Load a form definition by ID and optional version.

**Preconditions**: Form ID exists in manifest

**Domain Steps**:
1. Load FormAggregate
2. Invoke LoadFormDefinition(formId, version)
3. Verify FRM-004 (old versions not modifiable — read-only guarantee)

**Postconditions**: Form definition (fields, sections, validation rules) returned

**Invariant Guards (DOC-015)**:
- FRM-004 (versioning — old versions immutable)

**Domain Events Emitted (DOC-014)**: NONE

**Responses**:
- Success: FormDefinition (JSON representation of YAML/JSON form template)
- Error codes: E-404-004 (form not found)

**Cross-Aggregate Dependencies**: VocabularyAggregate (select options sourced from vocab)

**Idempotence**: YES

---

### UC-FRM-Q02 — RenderForm

| Propriete | Valeur |
|-----------|--------|
| Operation ID | UC-FRM-Q02 |
| Source API Contract | API-CONTRACT-001 §Form-3 |
| Source Command/Event | DOC-014 RenderForm / — |
| Aggregate | FormAggregate |
| Type | QUERY |
| RBAC Roles | any authenticated (form:read) |

**Objectif**: Produce a render tree for a form with conditional visibility evaluated.

**Preconditions**: Form definition loaded; optional data context

**Domain Steps**:
1. Load FormAggregate
2. Invoke RenderForm(formDef, data)
3. Verify FRM-001 (no hardcoded JSX), VOCAB-002 (select options from vocabulary only)

**Postconditions**: React Native render tree (JSON structure) produced

**Invariant Guards (DOC-015)**:
- FRM-001 (no hardcoded forms)
- VOCAB-002 (select from vocabulary only)

**Domain Events Emitted (DOC-014)**: NONE

**Responses**:
- Success: React Native render tree (JSON structure)
- Error codes: E-422-001-VOCAB-002

**Cross-Aggregate Dependencies**: VocabularyAggregate (select options)

**Idempotence**: YES

---

### UC-FRM-Q03 — GetVisibleFields

| Propriete | Valeur |
|-----------|--------|
| Operation ID | UC-FRM-Q03 |
| Source API Contract | API-CONTRACT-001 §Form-4 |
| Source Command/Event | DOC-014 (boundary-defined) / — |
| Aggregate | FormAggregate |
| Type | QUERY |
| RBAC Roles | any authenticated (form:read) |

**Objectif**: Return only form fields whose visible_if conditions match the given context.

**Preconditions**: Form definition loaded

**Domain Steps**:
1. Load FormAggregate
2. Invoke GetVisibleFields(formDef, context)
3. Verify FRM-003 (visible_if conditions evaluated)

**Postconditions**: Subset of FormField definitions returned

**Invariant Guards (DOC-015)**:
- FRM-003 (visible_if conditions supported)

**Domain Events Emitted (DOC-014)**: NONE

**Responses**:
- Success: Subset of FormField definitions
- Error codes: E-404-004

**Cross-Aggregate Dependencies**: NONE

**Idempotence**: YES

---

## NOTIFICATION AGGREGATE USE CASES

### UC-NOT-01 — SendNotification

| Propriete | Valeur |
|-----------|--------|
| Operation ID | UC-NOT-01 |
| Source API Contract | API-CONTRACT-001 §Notif-1 |
| Source Command/Event | DOC-014 SendNotification / NotificationQueued → NotificationSent |
| Aggregate | NotificationAggregate |
| Type | COMMAND |
| RBAC Roles | system (auto) or admin (notification:send) |

**Objectif**: Send a notification via the appropriate channel respecting preferences and rate limits.

**Preconditions**: Channel available; user exists; trigger present (NOT-001)

**Domain Steps**:
1. Load NotificationAggregate
2. Invoke SendNotification(userId, channel, body)
3. Verify NOT-001 (always triggered), NOT-002 (rate limit), CHANNEL-003 (preferences), QUIET-004 (quiet hours unless critical)

**Postconditions**: Notification sent or queued if offline

**Invariant Guards (DOC-015)**:
- NOT-001 (always triggered — never spontaneous)
- NOT-002 (rate limit configurable per user/org)
- CHANNEL-003 (user channel preferences respected)
- QUIET-004 (quiet hours unless critical severity)

**Domain Events Emitted (DOC-014)**:
- NotificationQueued: {notificationId, channel, recipientId}
- NotificationSent: {notificationId, channel, sentAt}
- NotificationFailed: {notificationId, channel, errorMessage, retryCount}

**Responses**:
- Success: void
- Error codes: E-422-001-NOT-001, E-422-001-RATE-002, E-422-001-CHANNEL-003, E-422-001-QUIET-004

**Cross-Aggregate Dependencies**: IdentityAggregate (user recipient), OfflineSyncAggregate (queue if offline)

**Idempotence**: NO — each send is distinct

---

### UC-NOT-02 — MarkAsRead

| Propriete | Valeur |
|-----------|--------|
| Operation ID | UC-NOT-02 |
| Source API Contract | API-CONTRACT-001 §Notif-2 |
| Source Command/Event | DOC-014 MarkAsRead / NotificationMarkedRead |
| Aggregate | NotificationAggregate |
| Type | COMMAND |
| RBAC Roles | self only (notification:read:self) |

**Objectif**: Mark a notification as read.

**Preconditions**: Notification belongs to the requesting user (self only)

**Domain Steps**:
1. Load NotificationAggregate
2. Invoke MarkAsRead(notificationId)
3. No specific invariants checked

**Postconditions**: Read timestamp set

**Invariant Guards (DOC-015)**: NONE

**Domain Events Emitted (DOC-014)**:
- NotificationMarkedRead: {notificationId, readAt}

**Responses**:
- Success: void
- Error codes: E-404-001

**Cross-Aggregate Dependencies**: NONE

**Idempotence**: YES

---

### UC-NOT-03 — UpdatePreferences

| Propriete | Valeur |
|-----------|--------|
| Operation ID | UC-NOT-03 |
| Source API Contract | API-CONTRACT-001 §Notif-3 |
| Source Command/Event | DOC-014 UpdatePreferences / PreferencesUpdated |
| Aggregate | NotificationAggregate |
| Type | COMMAND |
| RBAC Roles | self (notification:pref:self) or admin (notification:pref:any) |

**Objectif**: Update notification channel and rate preferences for a user.

**Preconditions**: Self or Admin; preferences valid JSON

**Domain Steps**:
1. Load NotificationAggregate
2. Invoke UpdatePreferences(userId, prefs)
3. Verify CHANNEL-003 (preferences respected going forward)

**Postconditions**: Preferences updated; channels validated against allowed set

**Invariant Guards (DOC-015)**:
- CHANNEL-003 (channel preferences respected)

**Domain Events Emitted (DOC-014)**:
- PreferencesUpdated: {userId, channels, severityMin, rateLimitH}

**Responses**:
- Success: void
- Error codes: E-403-001

**Cross-Aggregate Dependencies**: NONE

**Idempotence**: YES — updating same preferences is a no-op

---

### UC-NOT-04 — SetRateLimit

| Propriete | Valeur |
|-----------|--------|
| Operation ID | UC-NOT-04 |
| Source API Contract | API-CONTRACT-001 §Notif-4 |
| Source Command/Event | DOC-014 SetRateLimit / — |
| Aggregate | NotificationAggregate |
| Type | COMMAND |
| RBAC Roles | admin (notification:ratelimit) |

**Objectif**: Configure the rate limit for notifications per user/org.

**Preconditions**: Admin; max per hour > 0

**Domain Steps**:
1. Load NotificationAggregate
2. Invoke SetRateLimit(userId, maxPerHour)
3. Verify RATE-002

**Postconditions**: Rate limit applied

**Invariant Guards (DOC-015)**:
- RATE-002 (rate limit enforced)

**Domain Events Emitted (DOC-014)**: NONE explicitly (implicit preference update)

**Responses**:
- Success: void
- Error codes: E-403-001, E-400-002 (invalid value)

**Cross-Aggregate Dependencies**: NONE

**Idempotence**: YES

---

## VOCABULARY AGGREGATE USE CASES

### UC-VOC-01 — AddTermValue

| Propriete | Valeur |
|-----------|--------|
| Operation ID | UC-VOC-01 |
| Source API Contract | API-CONTRACT-001 §Vocab-1 |
| Source Command/Event | DOC-014 AddTermValue / TermAdded |
| Aggregate | VocabularyAggregate |
| Type | COMMAND |
| RBAC Roles | admin (vocabulary:manage) |

**Objectif**: Add a new term value to a namespace/term with unique key.

**Preconditions**: Namespace exists; key unique within namespace

**Domain Steps**:
1. Load VocabularyAggregate
2. Invoke AddTermValue(namespace, termKey, value)
3. Verify STABLE-003 (key unique within namespace, never changes), TRANSLATION-002 (min FR+EN)

**Postconditions**: New term value added with unique key

**Invariant Guards (DOC-015)**:
- STABLE-003 (keys stable forever)
- TRANSLATION-002 (minimum FR+EN translations)

**Domain Events Emitted (DOC-014)**:
- TermAdded: {namespace, termKey, valueKey}

**Responses**:
- Success: void
- Error codes: E-409-002 (duplicate key), E-422-001-TRANSLATION-002

**Cross-Aggregate Dependencies**: OrganizationAggregate (vocab namespaced per org with global fallback)

**Idempotence**: NO — key uniqueness enforced

---

### UC-VOC-02 — DeprecateTermValue

| Propriete | Valeur |
|-----------|--------|
| Operation ID | UC-VOC-02 |
| Source API Contract | API-CONTRACT-001 §Vocab-2 |
| Source Command/Event | DOC-014 DeprecateTermValue / TermValueDeprecated |
| Aggregate | VocabularyAggregate |
| Type | COMMAND |
| RBAC Roles | admin (vocabulary:manage) |

**Objectif**: Irreversibly mark a vocabulary value as deprecated.

**Preconditions**: Value exists; not already deprecated

**Domain Steps**:
1. Load VocabularyAggregate
2. Invoke DeprecateTermValue(namespace, termKey, value)
3. Verify VOC-001 (values never deleted, only deprecated — IRREVERSIBLE)

**Postconditions**: Value flagged deprecated; still accessible for legacy references

**Invariant Guards (DOC-015)**:
- VOC-001 (never delete values)

**Domain Events Emitted (DOC-014)**:
- TermValueDeprecated: {namespace, termKey, deprecatedValue}

**Responses**:
- Success: void
- Error codes: E-409-005 (already deprecated)

**Cross-Aggregate Dependencies**: NONE

**Idempotence**: YES — deprecating an already-deprecated value is a no-op

---

### UC-VOC-Q01 — ResolveLabel

| Propriete | Valeur |
|-----------|--------|
| Operation ID | UC-VOC-Q01 |
| Source API Contract | API-CONTRACT-001 §Vocab-3 |
| Source Command/Event | DOC-014 ResolveLabel / TranslationResolved |
| Aggregate | VocabularyAggregate |
| Type | QUERY |
| RBAC Roles | any authenticated (vocabulary:read) |

**Objectif**: Resolve a term display label for a specific language.

**Preconditions**: Namespace + termKey exist

**Domain Steps**:
1. Load VocabularyAggregate
2. Invoke ResolveLabel(namespace, termKey, lang)
3. Verify TRANSLATION-002 (FR+EN guaranteed minimum)

**Postconditions**: Display label string returned for requested language

**Invariant Guards (DOC-015)**:
- TRANSLATION-002 (min FR+EN)

**Domain Events Emitted (DOC-014)**:
- TranslationResolved: {namespace, termKey, lang, resolvedLabel}

**Responses**:
- Success: Label string
- Error codes: E-404-005 (vocab term not found)

**Cross-Aggregate Dependencies**: NONE (pure lookup)

**Idempotence**: YES

---

### UC-VOC-Q02 — GetTerms

| Propriete | Valeur |
|-----------|--------|
| Operation ID | UC-VOC-Q02 |
| Source API Contract | API-CONTRACT-001 §Vocab-4 |
| Source Command/Event | DOC-014 (boundary-defined) / — |
| Aggregate | VocabularyAggregate |
| Type | QUERY |
| RBAC Roles | any authenticated (vocabulary:read) |

**Objectif**: List all terms in a namespace.

**Preconditions**: Namespace exists

**Domain Steps**:
1. Load VocabularyAggregate
2. Invoke GetTerms(namespace)

**Postconditions**: All terms in namespace returned

**Invariant Guards (DOC-015)**: NONE

**Domain Events Emitted (DOC-014)**: NONE

**Responses**:
- Success: List of Term definitions
- Error codes: E-404-005

**Cross-Aggregate Dependencies**: NONE

**Idempotence**: YES

---

### UC-VOC-Q03 — GetTermValues

| Propriete | Valeur |
|-----------|--------|
| Operation ID | UC-VOC-Q03 |
| Source API Contract | API-CONTRACT-001 §Vocab-5 |
| Source Command/Event | DOC-014 (boundary-defined) / — |
| Aggregate | VocabularyAggregate |
| Type | QUERY |
| RBAC Roles | any authenticated (vocabulary:read) |

**Objectif**: List all values (including deprecated) for a namespace + term.

**Preconditions**: Namespace + termKey exist

**Domain Steps**:
1. Load VocabularyAggregate
2. Invoke GetTermValues(namespace, termKey)

**Postconditions**: All values including deprecated returned

**Invariant Guards (DOC-015)**: NONE

**Domain Events Emitted (DOC-014)**: NONE

**Responses**:
- Success: List of TermValue definitions
- Error codes: E-404-005

**Cross-Aggregate Dependencies**: NONE

**Idempotence**: YES

---

### UC-VOC-Q04 — SearchTerms

| Propriete | Valeur |
|-----------|--------|
| Operation ID | UC-VOC-Q04 |
| Source API Contract | API-CONTRACT-001 §Vocab-6 |
| Source Command/Event | DOC-014 (boundary-defined) / — |
| Aggregate | VocabularyAggregate |
| Type | QUERY |
| RBAC Roles | any authenticated (vocabulary:read) |

**Objectif**: Search for terms matching a query across optionally a specific namespace.

**Preconditions**: Search query provided

**Domain Steps**:
1. Load VocabularyAggregate
2. Invoke SearchTerms(query, namespace?)

**Postconditions**: Matching terms returned

**Invariant Guards (DOC-015)**: NONE

**Domain Events Emitted (DOC-014)**: NONE

**Responses**:
- Success: List of matching Term definitions
- Error codes: E-400-001 (query empty)

**Cross-Aggregate Dependencies**: NONE

**Idempotence**: YES

---

### UC-VOC-Q05 — GetAllNamespaces

| Propriete | Valeur |
|-----------|--------|
| Operation ID | UC-VOC-Q05 |
| Source API Contract | API-CONTRACT-001 §Vocab-7 |
| Source Command/Event | DOC-014 (boundary-defined) / — |
| Aggregate | VocabularyAggregate |
| Type | QUERY |
| RBAC Roles | any authenticated (vocabulary:read) |

**Objectif**: List all available vocabulary namespaces.

**Preconditions**: None

**Domain Steps**:
1. Load VocabularyAggregate
2. Invoke GetAllNamespaces()

**Postconditions**: All namespaces returned

**Invariant Guards (DOC-015)**: NONE

**Domain Events Emitted (DOC-014)**: NONE

**Responses**:
- Success: List of Namespace keys
- Error codes: NONE (no preconditions)

**Cross-Aggregate Dependencies**: NONE

**Idempotence**: YES

---

## REPORTING AGGREGATE USE CASES

### UC-RPT-01 — GenerateReport

| Propriete | Valeur |
|-----------|--------|
| Operation ID | UC-RPT-01 |
| Source API Contract | API-CONTRACT-001 §Report-1 |
| Source Command/Event | DOC-014 GenerateReport / ReportGenerated |
| Aggregate | ReportingAggregate |
| Type | COMMAND (produces report, does not persist) |
| RBAC Roles | admin, treasurer (reporting:generate) |

**Objectif**: Compute a financial report on-demand from approved transactions.

**Preconditions**: Report type defined; permission reporting:read held

**Domain Steps**:
1. Load ReportingAggregate
2. Invoke GenerateReport(reportType, period)
3. Verify BAL-001 (balance must balance), MONTH-001 (monthly covers 1st to last day), SYNCED-001 (only synced=1 transactions), EXPORT-001 (timestamp included)

**Postconditions**: Report computed and returned on-demand (not persisted)

**Invariant Guards (DOC-015)**:
- BAL-001 (balance: Actif = Passif + Résultat)
- MONTH-001 (monthly covers complete month)
- SYNCED-001 (only approved transactions participate)
- EXPORT-001 (export timestamped)

**Domain Events Emitted (DOC-014)**:
- ReportGenerated: {reportId, reportType, periodStart, periodEnd, income, expense, netResult, format}

**Responses**:
- Success: Generated report data
- Error codes: E-422-001 (BAL-001 balance violation)

**Cross-Aggregate Dependencies**: ResourceAggregate (reads TransactionRecord data), OrganizationAggregate (org scope)

**Idempotence**: YES — same report type + period produces identical result

---

### UC-RPT-Q01 — CalculateBalance

| Propriete | Valeur |
|-----------|--------|
| Operation ID | UC-RPT-Q01 |
| Source API Contract | API-CONTRACT-001 §Report-2 |
| Source Command/Event | DOC-014 CalculateBalance / BalanceCalculated |
| Aggregate | ReportingAggregate |
| Type | QUERY (read-only computation) |
| RBAC Roles | system (auto) or admin (reporting:calculate) |

**Objectif**: Compute balance totals from approved transactions filtered by scope and period.

**Preconditions**: Period defined

**Domain Steps**:
1. Load ReportingAggregate
2. Invoke CalculateBalance(scope, periodStart, periodEnd)
3. Verify BAL-001, MONTH-001, SYNCED-001

**Postconditions**: Balance totals computed from approved transactions

**Invariant Guards (DOC-015)**:
- BAL-001 (balance integrity)
- MONTH-001 (period validity)
- SYNCED-001 (only synced transactions)

**Domain Events Emitted (DOC-014)**:
- BalanceCalculated: {scope, periodStart, periodEnd, totals, categoryBreakdown}

**Responses**:
- Success: BalanceTotals with category breakdown
- Error codes: E-400-006 (invalid date range)

**Cross-Aggregate Dependencies**: ResourceAggregate (reads approved transactions)

**Idempotence**: YES

---

### UC-RPT-Q02 — ExportReport

| Propriete | Valeur |
|-----------|--------|
| Operation ID | UC-RPT-Q02 |
| Source API Contract | API-CONTRACT-001 §Report-3 |
| Source Command/Event | DOC-014 ExportReport / ReportExported |
| Aggregate | ReportingAggregate |
| Type | QUERY (read-only export) |
| RBAC Roles | admin, treasurer (reporting:export) |

**Objectif**: Export a previously generated report in a specified format.

**Preconditions**: Report previously generated; permission reporting:read

**Domain Steps**:
1. Load ReportingAggregate
2. Invoke ExportReport(reportId, format)
3. Verify EXPORT-001 (timestamp + digital signature), ARCHIVED-001 (archived reports immutable)

**Postconditions**: Export file returned in requested format

**Invariant Guards (DOC-015)**:
- EXPORT-001 (export timestamped + signed)
- ARCHIVED-001 (archived reports immutable)

**Domain Events Emitted (DOC-014)**:
- ReportExported: {reportId, format, exportedBy, exportedAt}

**Responses**:
- Success: Export file contents
- Error codes: E-404-001 (report not found)

**Cross-Aggregate Dependencies**: NONE (report already generated)

**Idempotence**: YES

---

### UC-RPT-Q03 — GetReportTypes

| Propriete | Valeur |
|-----------|--------|
| Operation ID | UC-RPT-Q03 |
| Source API Contract | API-CONTRACT-001 §Report-4 |
| Source Command/Event | DOC-014 (boundary-defined) / — |
| Aggregate | ReportingAggregate |
| Type | QUERY |
| RBAC Roles | any authenticated (reporting:read) |

**Objectif**: List available report types for an organization.

**Preconditions**: Org exists

**Domain Steps**:
1. Load ReportingAggregate
2. Invoke GetReportTypes(orgId)

**Postconditions**: Available report types returned

**Invariant Guards (DOC-015)**: NONE

**Domain Events Emitted (DOC-014)**: NONE

**Responses**:
- Success: List of report type definitions
- Error codes: E-404-002

**Cross-Aggregate Dependencies**: NONE

**Idempotence**: YES

---

## AUDIT AGGREGATE USE CASES

### UC-AUD-01 — LogAction

| Propriete | Valeur |
|-----------|--------|
| Operation ID | UC-AUD-01 |
| Source API Contract | API-CONTRACT-001 §Audit-1 |
| Source Command/Event | DOC-014 LogAction / ActionLogged (internal) |
| Aggregate | AuditAggregate |
| Type | COMMAND (SYSTEM ONLY, auto-invoked by other aggregates) |
| RBAC Roles | SYSTEM ONLY — not user-callable |

**Objectif**: Append an immutable audit log entry for a domain state change.

**Preconditions**: Any domain state change occurred (called by other aggregates, not user-facing)

**Domain Steps**:
1. Load AuditAggregate
2. Invoke LogAction(entityType, entityId, action, oldValues, newValues)
3. Verify AUD-001 (never modifiable/deletable), OLDNEW-002 (both old AND new values present), RETENTION-031 (retention policy set)

**Postconditions**: New immutable log entry appended

**Invariant Guards (DOC-015)**:
- AUD-001 (append-only, immutable)
- OLDNEW-002 (old AND new snapshots always present)
- RETENTION-031 (min 7 years retention)

**Domain Events Emitted (DOC-014)**:
- ActionLogged: {internal side effect — not user-facing}

**Responses**:
- Success: void
- Error codes: E-422-001-AUD-001, E-422-001-AUD-OLDNEW-002

**Cross-Aggregate Dependencies**: NONE (self-contained append-only)

**Idempotence**: NO — each action is distinct; system-only caller ensures ordering

---

### UC-AUD-Q01 — QueryAuditLogs

| Propriete | Valeur |
|-----------|--------|
| Operation ID | UC-AUD-Q01 |
| Source API Contract | API-CONTRACT-001 §Audit-2 |
| Source Command/Event | DOC-014 QueryAuditLogs / — |
| Aggregate | AuditAggregate |
| Type | QUERY |
| RBAC Roles | admin, auditor (audit:read) |

**Objectif**: Query audit log entries with filters scoped to org_id.

**Preconditions**: Filter criteria specified; admin or auditor permission held

**Domain Steps**:
1. Load AuditAggregate
2. Invoke QueryLogs(filters, pagination)
3. Verify ACCESS-033 (restricted to admin/auditor roles)

**Postconditions**: Log entries returned scoped to org_id

**Invariant Guards (DOC-015)**:
- ACCESS-033 (access restricted to admin/auditor)

**Domain Events Emitted (DOC-014)**: NONE

**Responses**:
- Success: List of AuditLogEntry records (read-only)
- Error codes: E-403-001

**Cross-Aggregate Dependencies**: NONE

**Idempotence**: YES

---

### UC-AUD-Q02 — ExportAuditTrail

| Propriete | Valeur |
|-----------|--------|
| Operation ID | UC-AUD-Q02 |
| Source API Contract | API-CONTRACT-001 §Audit-3 |
| Source Command/Event | DOC-014 (boundary-defined) / — |
| Aggregate | AuditAggregate |
| Type | QUERY |
| RBAC Roles | admin (audit:export) |

**Objectif**: Export audit trail data in a specified format for compliance.

**Preconditions**: Period specified; admin permission; min 7-year retention enforced

**Domain Steps**:
1. Load AuditAggregate
2. Invoke ExportAuditTrail(period, format)
3. Verify ACCESS-033, RETENTION-031 (min 7 years enforced in output)

**Postconditions**: Audit trail exported in requested format

**Invariant Guards (DOC-015)**:
- ACCESS-033
- RETENTION-031 (min 7 years)

**Domain Events Emitted (DOC-014)**: NONE

**Responses**:
- Success: Exported audit trail data
- Error codes: E-403-001

**Cross-Aggregate Dependencies**: NONE

**Idempotence**: YES

---

## LIFECYCLE AGGREGATE USE CASES

### UC-LIF-01 — ArchiveResource

| Propriete | Valeur |
|-----------|--------|
| Operation ID | UC-LIF-01 |
| Source API Contract | API-CONTRACT-001 §Lifecycle-1 |
| Source Command/Event | DOC-014 ArchiveResource / ResourceArchived |
| Aggregate | LifecycleAggregate |
| Type | COMMAND |
| RBAC Roles | admin (lifecycle:archive) |

**Objectif**: Archive a resource for long-term retention.

**Preconditions**: Resource exists; archivable type configured in manifest.lifecycle.types[]

**Domain Steps**:
1. Load LifecycleAggregate
2. Invoke ArchiveResource(resourceType, resourceId)
3. Verify LIF-001 (states configurable via manifest, not hardcoded), LIF-003 (purge irreversible once reached)

**Postconditions**: ArchiveEntry created with state = archived

**Invariant Guards (DOC-015)**:
- LIF-001 (states configurable per manifest)

**Domain Events Emitted (DOC-014)**:
- ResourceArchived: {archiveId, resourceType, resourceId, archivedAt, archivedBy}

**Responses**:
- Success: void
- Error codes: E-422-001-LIF-001

**Cross-Aggregate Dependencies**: ResourceAggregate (archives resources), IdentityAggregate (archived_by user)

**Idempotence**: NO — each archive creates a unique entry

---

### UC-LIF-02 — TrashResource

| Propriete | Valeur |
|-----------|--------|
| Operation ID | UC-LIF-02 |
| Source API Contract | API-CONTRACT-001 §Lifecycle-2 |
| Source Command/Event | DOC-014 TrashResource / ResourceTrashed |
| Aggregate | LifecycleAggregate |
| Type | COMMAND |
| RBAC Roles | admin (lifecycle:trash) |

**Objectif**: Move an archived entry to trash.

**Preconditions**: ArchiveEntry exists; state = archived

**Domain Steps**:
1. Load LifecycleAggregate
2. Invoke TrashResource(archiveId)
3. Verify LIF-003 (must not have been purged yet)

**Postconditions**: State transitions archived → trashed

**Invariant Guards (DOC-015)**:
- LIF-003 (purge irreversible — entry must not be purged)

**Domain Events Emitted (DOC-014)**:
- ResourceTrashed: {archiveId, trashedAt, trashReason}

**Responses**:
- Success: void
- Error codes: E-409-006 (already purged)

**Cross-Aggregate Dependencies**: NONE

**Idempotence**: NO

---

### UC-LIF-03 — PurgeResource

| Propriete | Valeur |
|-----------|--------|
| Operation ID | UC-LIF-03 |
| Source API Contract | API-CONTRACT-001 §Lifecycle-3 |
| Source Command/Event | DOC-014 PurgeResource / ResourcePurged |
| Aggregate | LifecycleAggregate |
| Type | COMMAND (SYSTEM ONLY — scheduled, never user-callable) |
| RBAC Roles | SYSTEM ONLY (scheduled) |

**Objectif**: Permanently remove an archived entry (IRREVERSIBLE).

**Preconditions**: System scheduled; trash date passed; purge_date reached

**Domain Steps**:
1. Load LifecycleAggregate
2. Invoke PurgeResource(archiveId)
3. Verify LIF-003 (irreversible — purged blocks ALL transitions), LIF-005 (purge_date reached)

**Postconditions**: Entry permanently removed. NO RESTORE possible.

**Invariant Guards (DOC-015)**:
- LIF-003 (purge irreversible)
- LIF-005 (purge date configurable per type)

**Domain Events Emitted (DOC-014)**:
- ResourcePurged: {archiveId, purgedAt}

**Responses**:
- Success: void
- Error codes: E-422-001-LIF-003, E-422-001-LIF-005

**Cross-Aggregate Dependencies**: NONE

**Idempotence**: YES — purging an already-purged entry is a no-op

---

### UC-LIF-04 — RestoreFromTrash

| Propriete | Valeur |
|-----------|--------|
| Operation ID | UC-LIF-04 |
| Source API Contract | API-CONTRACT-001 §Lifecycle-4 |
| Source Command/Event | DOC-014 RestoreFromTrash / ResourceRestoredFromTrash |
| Aggregate | LifecycleAggregate |
| Type | COMMAND |
| RBAC Roles | admin (lifecycle:restore) |

**Objectif**: Restore a trashed entry back to archived state.

**Preconditions**: Entry in trashed state

**Domain Steps**:
1. Load LifecycleAggregate
2. Invoke RestoreFromTrash(archiveId)
3. Verify LIF-003 (only works before purged)

**Postconditions**: State transitions trashed → archived

**Invariant Guards (DOC-015)**:
- LIF-003 (must not be purged)

**Domain Events Emitted (DOC-014)**:
- ResourceRestoredFromTrash: {archiveId, restoredAt, restoredBy}

**Responses**:
- Success: void
- Error codes: E-409-006 (already purged)

**Cross-Aggregate Dependencies**: NONE

**Idempotence**: YES — restoring an already-restored entry is a no-op

---

### UC-LIF-Q01 — ListArchiveEntries

| Propriete | Valeur |
|-----------|--------|
| Operation ID | UC-LIF-Q01 |
| Source API Contract | API-CONTRACT-001 §Lifecycle-5 |
| Source Command/Event | DOC-014 ListArchiveEntries / — |
| Aggregate | LifecycleAggregate |
| Type | QUERY |
| RBAC Roles | admin (lifecycle:read) |

**Objectif**: List archive entries with filters; trashed excluded from normal query.

**Preconditions**: Admin permission

**Domain Steps**:
1. Load LifecycleAggregate
2. Invoke ListArchiveEntries(filters)
3. Verify LIF-006 (trashed not visible in normal queries)

**Postconditions**: Archive entries returned; trashed excluded

**Invariant Guards (DOC-015)**:
- LIF-006 (trashed entries excluded from normal queries)

**Domain Events Emitted (DOC-014)**: NONE

**Responses**:
- Success: List of ArchiveEntry records
- Error codes: E-403-001

**Cross-Aggregate Dependencies**: NONE

**Idempotence**: YES

---

### UC-LIF-Q02 — SearchArchives

| Propriete | Valeur |
|-----------|--------|
| Operation ID | UC-LIF-Q02 |
| Source API Contract | API-CONTRACT-001 §Lifecycle-6 |
| Source Command/Event | DOC-014 (boundary-defined) / — |
| Aggregate | LifecycleAggregate |
| Type | QUERY |
| RBAC Roles | admin (lifecycle:read) |

**Objectif**: Search archives by query, tags, and/or type.

**Preconditions**: Admin permission

**Domain Steps**:
1. Load LifecycleAggregate
2. Invoke SearchArchives(query, tags?, type?)

**Postconditions**: Matching archive entries returned

**Invariant Guards (DOC-015)**: NONE

**Domain Events Emitted (DOC-014)**: NONE

**Responses**:
- Success: List of matching ArchiveEntry records
- Error codes: E-403-001

**Cross-Aggregate Dependencies**: NONE

**Idempotence**: YES

---

## CONFIGURATION AGGREGATE USE CASES

### UC-CFG-01 — UpdateSetting

| Propriete | Valeur |
|-----------|--------|
| Operation ID | UC-CFG-01 |
| Source API Contract | API-CONTRACT-001 §Config-1 |
| Source Command/Event | DOC-014 UpdateSetting / SettingUpdated |
| Aggregate | ConfigurationAggregate |
| Type | COMMAND |
| RBAC Roles | admin (config:update) |

**Objectif**: Update a single organization configuration setting.

**Preconditions**: Key exists in settings schema; Admin permission

**Domain Steps**:
1. Load ConfigurationAggregate
2. Invoke UpdateSetting(key, value)
3. Verify CFG-001 (ISO 4217 currency), CFG-002 (IANA timezone), CFG-003 (hex + WCAG)

**Postconditions**: Setting value updated with format validation

**Invariant Guards (DOC-015)**:
- CFG-001 (currency ISO 4217)
- CFG-002 (timezone IANA)
- CFG-003 (accent color hex + WCAG contrast)

**Domain Events Emitted (DOC-014)**:
- SettingUpdated: {settingKey, oldValue, newValue, updatedAt}

**Responses**:
- Success: void
- Error codes: E-422-001-CFG-001, E-422-001-CFG-002, E-422-001-CFG-003

**Cross-Aggregate Dependencies**: OrganizationAggregate (settings org-scoped)

**Idempotence**: YES — updating same key/value pair is idempotent

---

### UC-CFG-02 — ResetToDefaults

| Propriete | Valeur |
|-----------|--------|
| Operation ID | UC-CFG-02 |
| Source API Contract | API-CONTRACT-001 §Config-2 |
| Source Command/Event | DOC-014 ResetToDefaults / SettingsResetToDefaults |
| Aggregate | ConfigurationAggregate |
| Type | COMMAND |
| RBAC Roles | admin (config:reset) |

**Objectif**: Reset all organization settings to template defaults.

**Preconditions**: Admin permission; settings not already at defaults

**Domain Steps**:
1. Load ConfigurationAggregate
2. Invoke ResetToDefaults()
3. Verify CFG-004 (every setting has a default)

**Postconditions**: All keys reset to template defaults

**Invariant Guards (DOC-015)**:
- CFG-004 (default fallback for every setting)

**Domain Events Emitted (DOC-014)**:
- SettingsResetToDefaults: {resetAt, resetBy}

**Responses**:
- Success: void
- Error codes: E-403-001

**Cross-Aggregate Dependencies**: NONE

**Idempotence**: YES — resetting when already at defaults is a no-op

---

### UC-CFG-Q01 — GetSetting

| Propriete | Valeur |
|-----------|--------|
| Operation ID | UC-CFG-Q01 |
| Source API Contract | API-CONTRACT-001 §Config-3 |
| Source Command/Event | DOC-014 (boundary-defined) / — |
| Aggregate | ConfigurationAggregate |
| Type | QUERY |
| RBAC Roles | admin (config:read) |

**Objectif**: Retrieve a single setting value.

**Preconditions**: Key exists

**Domain Steps**:
1. Load ConfigurationAggregate
2. Invoke GetSetting(key)
3. Verify CFG-004 (default fallback if null)

**Postconditions**: Setting value returned; default fallback if null

**Invariant Guards (DOC-015)**:
- CFG-004 (default fallback)

**Domain Events Emitted (DOC-014)**: NONE

**Responses**:
- Success: Single SettingValue
- Error codes: E-404-001

**Cross-Aggregate Dependencies**: NONE

**Idempotence**: YES

---

### UC-CFG-Q02 — GetAllSettings

| Propriete | Valeur |
|-----------|--------|
| Operation ID | UC-CFG-Q02 |
| Source API Contract | API-CONTRACT-001 §Config-4 |
| Source Command/Event | DOC-014 (boundary-defined) / — |
| Aggregate | ConfigurationAggregate |
| Type | QUERY |
| RBAC Roles | admin (config:read) |

**Objectif**: Retrieve all organization settings with current values.

**Preconditions**: Admin permission

**Domain Steps**:
1. Load ConfigurationAggregate
2. Invoke GetAllSettings()
3. Verify CFG-004 (default fallback for any null values)

**Postconditions**: All settings returned with current values

**Invariant Guards (DOC-015)**:
- CFG-004 (default fallback)

**Domain Events Emitted (DOC-014)**: NONE

**Responses**:
- Success: List of all SettingEntries
- Error codes: E-403-001

**Cross-Aggregate Dependencies**: NONE

**Idempotence**: YES

---

## OFFLINE SYNC AGGREGATE USE CASES

### UC-SYNC-01 — PushPendingOperations

| Propriete | Valeur |
|-----------|--------|
| Operation ID | UC-SYNC-01 |
| Source API Contract | API-CONTRACT-001 §Sync-1 |
| Source Command/Event | DOC-014 PushPendingOperations / BatchPushed |
| Aggregate | OfflineSyncAggregate |
| Type | COMMAND (SYSTEM ONLY — auto) |
| RBAC Roles | SYSTEM (sync:push) — never user-initiated |

**Objectif**: Push local pending operations to remote server in batches.

**Preconditions**: Pending operations exist in local queue

**Domain Steps**:
1. Load OfflineSyncAggregate
2. Invoke PushPendingOperations()
3. Verify SYNC-001 (local ALWAYS precedes remote), SYNC-002 (batch ≤50), SYNC-003 (exponential backoff max 5 retries)

**Postconditions**: Batch pushed to remote; sync_status transitions to sent

**Invariant Guards (DOC-015)**:
- SYNC-001 (local first absolute)
- SYNC-002 (batch size max 50)
- SYNC-003 (retry exponential backoff max 5)

**Domain Events Emitted (DOC-014)**:
- BatchPushed: {batchCount, pushedAt}

**Responses**:
- Success: void
- Error codes: E-422-001-SYNC-002 (batch exceeded)

**Cross-Aggregate Dependencies**: ALL aggregates (pending operations track changes to every resource type)

**Idempotence**: YES — batching is designed to be safely retried

---

### UC-SYNC-02 — PullRemoteChanges

| Propriete | Valeur |
|-----------|--------|
| Operation ID | UC-SYNC-02 |
| Source API Contract | API-CONTRACT-001 §Sync-2 |
| Source Command/Event | DOC-014 PullRemoteChanges / DeltaReceived |
| Aggregate | OfflineSyncAggregate |
| Type | COMMAND (SYSTEM ONLY — auto) |
| RBAC Roles | SYSTEM (sync:pull) — never user-initiated |

**Objectif**: Pull delta changes from remote since last sync timestamp.

**Preconditions**: Network available; since timestamp provided

**Domain Steps**:
1. Load OfflineSyncAggregate
2. Invoke PullRemoteChanges(sinceTimestamp)
3. Verify SYNC-004 (user operations never blocked during sync)

**Postconditions**: Delta changes pulled; applied locally

**Invariant Guards (DOC-015)**:
- SYNC-004 (user ops never depend on sync synchronously)

**Domain Events Emitted (DOC-014)**:
- DeltaReceived: {table, count, sinceTimestamp}

**Responses**:
- Success: void
- Error codes: E-422-001-SYNC-004

**Cross-Aggregate Dependencies**: ALL aggregates (delta applies to resource types across all aggregates)

**Idempotence**: YES — pull with same sinceTimestamp always returns same delta

---

### UC-SYNC-03 — ResolveConflict

| Propriete | Valeur |
|-----------|--------|
| Operation ID | UC-SYNC-03 |
| Source API Contract | API-CONTRACT-001 §Sync-3 |
| Source Command/Event | DOC-014 ResolveConflict / ConflictResolved + ConflictDetected |
| Aggregate | OfflineSyncAggregate |
| Type | COMMAND (SYSTEM auto or Admin manual) |
| RBAC Roles | system (auto) or admin (sync:resolve) |

**Objectif**: Resolve a sync conflict between local and server data.

**Preconditions**: Conflict detected during pull

**Domain Steps**:
1. Load OfflineSyncAggregate
2. Invoke ResolveConflict(operation, serverData)
3. Verify SYNC-001 (local-first strategy: approved transactions immutable, drafts use UUID dedup + side-by-side diff)

**Postconditions**: Conflict resolved per strategy (LWW, server-wins, immutable, uuid-dedup, side-by-side)

**Invariant Guards (DOC-015)**:
- SYNC-001 (local-first absolute)

**Domain Events Emitted (DOC-014)**:
- ConflictDetected: {resourceType, resourceId, clientVersion, serverVersion}
- ConflictResolved: {resourceType, resourceId, winner (client/server), strategyUsed}

**Responses**:
- Success: void
- Error codes: E-422-001-SYNC-001

**Cross-Aggregate Dependencies**: ResourceAggregate (conflict data is resource-type specific)

**Idempotence**: YES — same conflict + same strategy yields same resolution

---

### UC-SYNC-04 — MarkOperationConfirmed

| Propriete | Valeur |
|-----------|--------|
| Operation ID | UC-SYNC-04 |
| Source API Contract | API-CONTRACT-001 §Sync-4 |
| Source Command/Event | DOC-014 (boundary-defined) / SyncCompleted |
| Aggregate | OfflineSyncAggregate |
| Type | COMMAND (SYSTEM ONLY — auto) |
| RBAC Roles | SYSTEM (sync:confirm) — never user-initiated |

**Objectif**: Confirm that a pending operation was successfully synced.

**Preconditions**: Operation successfully synced to remote

**Domain Steps**:
1. Load OfflineSyncAggregate
2. Invoke MarkConfirmed(opId)

**Postconditions**: Sync status transitions sent → confirmed

**Invariant Guards (DOC-015)**: NONE explicitly

**Domain Events Emitted (DOC-014)**:
- SyncCompleted: {orgId, completedAt, pushCount, pullCount}

**Responses**:
- Success: void
- Error codes: E-404-001

**Cross-Aggregate Dependencies**: ALL aggregates (completion scoped to org)

**Idempotence**: YES

---

### UC-SYNC-Q01 — CheckConnectivity

| Propriete | Valeur |
|-----------|--------|
| Operation ID | UC-SYNC-Q01 |
| Source API Contract | API-CONTRACT-001 §Sync-5 |
| Source Command/Event | DOC-014 (boundary-defined) / ConnectionLost or ConnectionRestored |
| Aggregate | OfflineSyncAggregate |
| Type | QUERY |
| RBAC Roles | any authenticated (sync:status) |

**Objectif**: Return the current connectivity state.

**Preconditions**: None

**Domain Steps**:
1. Load OfflineSyncAggregate
2. Invoke CheckConnectivity()
3. Verify SYNC-004 (never blocks)

**Postconditions**: Current connectivity state returned

**Invariant Guards (DOC-015)**:
- SYNC-004 (never blocks user)

**Domain Events Emitted (DOC-014)**:
- ConnectionLost or ConnectionRestored (as state changes)

**Responses**:
- Success: Connectivity state
- Error codes: NONE

**Cross-Aggregate Dependencies**: NONE

**Idempotence**: YES

---

### UC-SYNC-Q02 — GetSyncStatus

| Propriete | Valeur |
|-----------|--------|
| Operation ID | UC-SYNC-Q02 |
| Source API Contract | API-CONTRACT-001 §Sync-6 |
| Source Command/Event | DOC-014 (boundary-defined) / — |
| Aggregate | OfflineSyncAggregate |
| Type | QUERY |
| RBAC Roles | any authenticated (sync:status) |

**Objectif**: Return current sync status for a specific table/entity type.

**Preconditions**: Table name specified

**Domain Steps**:
1. Load OfflineSyncAggregate
2. Invoke GetSyncStatus(tableName)

**Postconditions**: Current sync status for table returned

**Invariant Guards (DOC-015)**: NONE

**Domain Events Emitted (DOC-014)**: NONE

**Responses**:
- Success: SyncStatusTracker data
- Error codes: E-400-004 (invalid table name)

**Cross-Aggregate Dependencies**: NONE

**Idempotence**: YES

---

## SUMMARY STATISTICS

| Metric | Count |
|--------|-------|
| Total use cases | 83 |
| Command use cases | 57 |
| Query use cases | 26 |
| Aggregates covered | 13 |
| Unique invariants referenced | 58 (all of DOC-015) |
| Unique domain events referenced | 60+ (from DOC-014) |
| Invented operations | 0 |
| Invented events | 0 |
| Invented invariants | 0 |

**Compliance statement**: Every use case traces to exactly ONE operation from API-CONTRACT-001. Every invariant guard traces to DOC-015. Every event traces to DOC-014. No concept, capability, aggregate, command, event, or invariant was invented.
