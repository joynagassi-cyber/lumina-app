/**
 * Domain Events barrel — base classes and shared types.
 */

export { DomainEvent } from '../../../../shared/events';
export type { DomainEventHandler } from '../../../../shared/events/handler.interface';
