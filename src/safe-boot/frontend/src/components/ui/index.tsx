/**
 * UI Component Skeletons — ITS-V1 Design System Base
 */

import { Pressable, Text, View } from 'react-native';

export function Button(_props: Record<string, unknown>) {
  return (
    <View>
      {/* TODO: Implement Button per Lumina design tokens */}
    </View>
  );
}

export function Input(_props: Record<string, unknown>) {
  return (
    <View>
      {/* TODO: Implement Input per Lumina design tokens */}
    </View>
  );
}

export function Card(_props: Record<string, unknown>) {
  return (
    <View>
      {/* TODO: Implement Card per Lumina design tokens */}
    </View>
  );
}

export function Icon(_props: Record<string, unknown>) {
  return (
    <Pressable>
      {/* TODO: Implement Icon per Lucide React Native */}
    </Pressable>
  );
}
